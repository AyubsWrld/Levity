#pragma once

// Generating an asymmetric key-pair
// knowing the public key does not let someone feasibly determine the private key.
// Private key creates signature -> public key validates the signature
